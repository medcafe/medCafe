#if ($repositoryList)
#set ($printFirst = 0)
#set ($patcnt = 0)
	#foreach ($repos in $repositoryList)
		#if ($printFirst == 0)
			#set ($printFirst = 1)	
			#if ($repos.procedures)
				{ "titleLabels": ["Type","Description","Date"],
				 "modelNames": ["Type","Description","Date"],
				"patientData":[
				#foreach(  $procDetail in $repos.procedures)
    		   	    #if ($patcnt > 0)
    		   	    ,
    		   	    #end
    		   	    #set ($patcnt = $patcnt+1)
    		   	    {"id":"$patcnt"
                    
    		   		#if ($procDetail.mood_code)
    		   		     ,"Type": "$procDetail.mood_code" 
                    #end
    		   		#if ($procDetail.description)
    		  		    ,"Description": "$procDetail.description" 
    		  			
    					#if($procDetail.time)   
    			          , "Date":"$procDetail.time"
    			               
    		            #end
    		            }
    	   			#end
    	   		#end
	   			]}
	   		#end
		#end
	#end
#end



