#set( $count=0)
#foreach(  $problem in $patientProblem )
	
	#if( $count == 0 )
		<table cellpadding="0" cellspacing="0" border="0" class="display" id="problemList$problem.patient_id">
		<thead><tr><th>Problem Title</th><th>Note</th><th>Priority</th></tr></thead><tbody>
	#end
	<tr class="gradeX">
	<td value="$problem.title">$problem.title</td>
	<td value="$problem.note">$problem.note</td>
	<td value="$problem.priority">$problem.priority</td>
	</tr>
	#set( $count = $count + 1 )
	
#end
</tbody>
<table>
