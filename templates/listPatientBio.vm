#if ($repositoryList)
   #set ($printed=0)
   #set ($space=" ")
   #foreach ($repos in $repositoryList)
   	#if ($repos.patient_data)
   		#if ($printed == 0)
   			<tr><th colspan="2" style="text-align:left;">
   			<b>$repos.patient_data.lastName</b>
   			$repos.patient_data.firstName </th></tr>
   			#set ($printed = 1)
   			#if ($repos.patient_data.info)
   			  <tr><td colspan="2">
				$repos.patient_data.demographic_info
     			  </td></tr>
			#end
     		#end
     	#end
	#end
#end     	
