#if ($repositoryList)
#set ($printFirst = 0)
#set ($patcnt = 0)
	#foreach ($repos in $repositoryList)
		#if ($printFirst == 0)
			#set ($printFirst = 1)	
			#if ($repos.vitals)
				{ "titleLabels": ["Type","Value","Date"],
				 "modelNames": ["Type","Value","VitalDate"],
				"patientData":[
				#foreach(  $vitalDetail in $repos.vitals)
    		   	    #if ($patcnt > 0)
    		   	    ,
    		   	    #end
    		   	    #set ($patcnt = $patcnt+1)
    		   	    {"id":"$patcnt"
    		   		#if ($vitalDetail.description)
    		   		     ,"Type": "$vitalDetail.description" 
                    #end
                    
                    #foreach ( $value in $vitalDetail.values)
                        
                        #if ($value.scalar)
                            #if ($value.scalar.length > 10)
                                 ,"Value": "$value.scalar.substring(0,6)"
                            #else
                                ,"Value": "$value.scalar"
                            #end
                        #end
                    #end
    		   		
    				#if($vitalDetail.time)
    		               	
    			         , "VitalDate":"$vitalDetail.time"
    			               
    		        #end
    	   			}
    	   		#end
	   			]}
	   		#end
		#end
	#end
#end



